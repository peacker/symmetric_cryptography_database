TODO:

- add pbkfd2 pdf


- Proposed visualizations (requested: "propose other interesting visualizations"; see DONE section for the
  request itself). None of these are implemented yet -- flagging as new tasks per the request:

  + Construction taxonomy treemap/sunburst: root construction -> leaf, box/wedge size = family count using
    it. An alternative entry point to browsing constructions vs. the flat checklist filter, and makes which
    taxonomies are "big families" (SPN, Feistel) vs. niche (single-leaf) visible at a glance.

  + Construction-adoption-over-time stream/stacked-area chart: per 5-year bucket, how many *new* families
    adopted each top-level construction. Complements the Timelines scatter with an aggregate trend view --
    would show things like the ARX/permutation-based rise post-2010 or Feistel's relative decline.

  + Parameter-space scatter: block/state size vs. key size (or vs. rounds), one point per instance, colored
    by type/construction/era. Should visually cluster lightweight designs (small block/key) apart from
    general-purpose/high-security ones, and make outliers (e.g. unusually many rounds for a small state)
    jump out.

  + Standardization-process attrition funnel/Sankey: per process (AES, SHA-3, CAESAR, NIST-LWC, eSTREAM,
    PHC, NESSIE), submitted -> round 1 -> round 2 -> finalist -> winner, sized by family count at each
    stage. The processes data already supports this; it would visualize the elimination funnel instead of
    only a flat participant list per stage.

  + Component usage heatmap: components (sbox, xor, lfsr, mds matrix, ...) x families or x decade, shaded
    by usage count. Gives a visual overview of which building blocks are common/rare and when they
    appeared, complementing the existing text-based "Component id/name contains" search in the Query
    Builder.

  + Round component-flow-signature similarity view: cluster/group families whose
    round_component_flow_signatures are identical or near-identical (already filterable by exact substring
    in the Query Builder) into a dendrogram or grouped list, to surface "structurally similar round"
    families that don't have a hand-tagged influence edge between them.

- Enc-then-Hash, Hash-then-Enc, Enc-and-Hash AEAD types
  Note: "Encrypt-then-MAC Composition" already exists as a construction (mode_constructions.yaml)
  Unclear on two points before I add these:
  1. Where do they belong? mode_types.yaml is a flat list (no sub-categories currently exist
     there), while mode_constructions.yaml already has the one directly analogous entry
     (encrypt_then_mac) as a root/leaf with no special_case_of. Do you want new sibling
     mode_constructions.yaml entries (hash_then_encrypt, encrypt_and_hash, alongside the existing
     encrypt_then_mac), or an actual new nested "AEAD types" level in mode_types.yaml (which
     doesn't exist yet, so it'd be a schema change, not just a data one)?
  2. "Hash" vs "MAC": a bare unkeyed hash can't provide unforgeability the way encrypt_then_mac
     needs (encrypt_then_mac's own note already generalizes "MAC" to include a keyed universal
     hash). Do you mean these three as literal unkeyed-hash compositions, or as a differently-named
     restatement of the classical Bellare-Namprempre generic-composition trio (Encrypt-and-MAC /
     MAC-then-Encrypt / Encrypt-then-MAC), just using "Hash" as the label?

- Sponge construction:
  what is the difference between Sponge Construction (general/unspecified) and Standard Sponge?
  we could add full rate absorbing and squeezing, full rate absorbing only, full rate squeezing,
  and sue standard sponge to mean the rest.
  are there other types of sponge?
  Still open -- this reads like you were thinking out loud rather than requesting a specific
  taxonomy, so I didn't want to invent leaf names unilaterally. If you want me to take a pass at
  it, let me know whether the intended split is by absorb/squeeze rate (full vs. partial) as you
  sketched, or something else (keyed vs. unkeyed, overwrite-mode sponges, etc.).

- Not touched, minor: primitive_constructions.yaml's typical_primitive_types field
  lists stream_cipher on several entries even though stream_cipher isn't a primitive type
  (it's mode-tier) — it's a pure documentation field, unused by the actual site build,
  so it doesn't affect the genealogy graph. Happy to clean it up if you want.
  -> Done, see DONE section.

- CGEN prng (based on a block cipher): official source only shows 2 pages: https://link.springer.com/chapter/10.1007/11958239_3
  ask someone with institutional access

- [ ]	PEA	PEA	block_cipher	TODO	TODO	TODO — spec not found	no
  -> seems a quantum based block cipher, out of scope for this database: https://openaccess.city.ac.uk/id/eprint/2509/1/PEA%20Polymorphic%20Encryption%20Algorithm.pdf
  Unclear: the PEA entry currently in the database (primitive_families.yaml, id "pea") is a
  lightweight SPN block cipher with placeholder/unconfirmed parameters -- it doesn't look
  related to the quantum-inspired "Polymorphic Encryption Algorithm" PDF linked above. Is this
  note flagging that PDF as a *checked-and-rejected* candidate source for the real PEA spec (i.e.
  still need to find the actual lightweight-PEA paper elsewhere), or is it saying the current
  database entry is itself the wrong PEA and should be removed/replaced? I didn't want to delete
  or rewrite a family entry on a guess.

- LEA, Salsa, ChaCha use a "cascade"-type of construction.
  which other ciphers have it?
  which was first?
  gimli??? -> to be checked
  Partially resolved already: LEA's own influences note (data/primitive_families.yaml, id "lea",
  influence edge to salsa20) already analyzes this in detail and concludes LEA's round is NOT
  actually a cascade (its three word-pair computations are parallel/independent within a round);
  Salsa20's quarterround IS a strict cascade (each step depends on the previous step's output).
  Still open: a broader survey of *other* ciphers with a genuine cascade-shaped round (and which
  was first -- the "gimli???" guess is unconfirmed) wasn't done and would need dedicated research
  rather than a quick check; let me know if you want that as its own task.

- add references to the constructions.yaml entries
  Scope check before I touch ~150 entries: primitive_constructions.schema.json and
  mode_constructions.schema.json have no reference_ids field at all right now (unlike the family
  schemas) -- every existing citation in these two files is an inline mention inside `notes`
  (e.g. "Hoang and Rogaway, 'On Generalized Feistel Networks,' CRYPTO 2010"), never a linked
  reference_ids array. Do you want (a) a schema change adding a proper reference_ids array plus
  back-filling it citation-by-citation across every entry (a large task), or (b) just more inline
  citations added to entries that are currently missing one, matching the existing convention?
  If (b), is there a subset of entries you care about most, or truly all ~150?


DONE:

- Eighteenth follow-up round: you noticed the AES'-128/128 (mixFeed's underlying block cipher) node tooltip in
  the Genealogy tab showed only Type/Construction/Process, missing the crucial "MixColumns also applied in the
  final round (unlike standard AES)" detail that actually explains what makes it a distinct cipher -- without it
  the tooltip makes AES'-128/128 look like plain AES with a different name. The detail was already well
  documented in the data (the family's own notes, its influences[].note back to aes, and the instance's own
  notes), just not surfaced in this specific tooltip. Root cause: the Genealogy tab's node tooltip is built in
  two near-duplicate places, one per layout (drawSugiyama, drawRadial) -- the layered layout's version already
  appended the family's notes field, but the radial layout's (the default layout) never did, so any node's
  notes were invisible unless you happened to switch to layered. Fixed by adding the same
  `...(fam.notes ? [fam.notes] : [])` the layered version already had. Verified live: AES'-128/128's radial-view
  tooltip now ends with "Chakraborty and Nandi 2019. AES'-128/128 (AES-128 with MixColumns added to the final
  round) is the block cipher underlying the mixFeed AEAD mode...". make test all green.

- Seventeenth follow-up round: added HKDF (the HMAC-based extract-then-expand key derivation function) as a new
  mode family, from two files you added -- Krawczyk's original CRYPTO 2010 paper (hkdf_ref) and its IETF
  standardization, RFC 5869 (krawczyk_eronen_2010_rfc5869). Modeled closely on the existing PBKDF2 entry: type
  kdf (not pbkdf, since HKDF has no deliberate password-cracking-resistance cost), construction_ids
  [hmac_construction, iterated_hash_kdf] (both of HKDF's extract and expand phases are literally HMAC calls), an
  influence edge to hmac_sha256 explaining that relationship, and underlying_primitive_ids: [sha256_v1] mirroring
  how hmac_sha256/pbkdf2 both trace through to the raw hash rather than stopping at HMAC. instances[0]
  characteristics use RFC 5869's own L <= 255*HashLen bound for the SHA-256 case (max 65280 bits). make test all
  green (497 families, up from 496); verified in the browser that both reference PDFs resolve under the "hkdf"
  family id and that searching "HKDF" at the default relation-degree of 1 correctly surfaces PBKDF2 too -- not a
  BFS bug, PBKDF2 and HKDF are genuine *direct* neighbors via the auto-derived "shares construction:
  iterated_hash_kdf" edge (both are iterated-hash KDFs), which is exactly the kind of direct relation degree=1
  should show.

- Sixteenth follow-up round: two more bugs you found.
  1. The relation picker's rows overlapped illegibly whenever a dense hub produced many candidates (a
     screenshot showed ~20 rows of text bleeding into each other). Root cause: .gen-relation-picker is a
     flex column with max-height + overflow-y:auto, but its items never got flex-shrink:0 -- with more
     items than fit in max-height, flexbox's default flex-shrink:1 squeezes every row down to fit
     *inside* the box instead of letting the box's own overflow-y:auto scroll, collapsing each row to
     ~11px (measured) regardless of its actual text content. Fixed with flex-shrink:0 on
     .gen-relation-picker-item; reproduced the exact 54-candidate NewDES hub from before and confirmed
     each row is now a proper ~26px with the container correctly scrolling (256px visible of 1550px
     total content) instead of squeezing.
  2. The Timelines plot didn't fit its container by default on a fresh page load/refresh (confirmed via
     screenshot and reproduced with Playwright: SVG rendered 1195px wide inside an 870px container).
     Traced to *two* stacked initialization-order bugs, found by instrumenting the live fit/zoom
     functions with console.log and tracing actual call order rather than guessing from static reading:
     (a) setupNavigator() ran first and immediately activated the default "visualizations" tab, which
     tries to fit the plot via viewRefreshHooks.visualizations -- but that hook isn't registered until
     setupFamilyVisualization() runs, several calls later, so the very first (and only automatic) fit
     attempt silently no-op'd forever. (b) once (a) was fixed, tracing revealed a second, previously-
     masked bug: setupFullscreenAndFilterToggles() sets each view's default "Filters on left" side-layout
     class on a large landscape screen, which narrows the plot's actual container width -- but
     setupFamilyVisualization()'s own initial render() (which does its own fit-to-width, independent of
     the navigator) was running *before* that class got applied, so it fit to the wider pre-side-layout
     width, and nothing ever re-fit afterward (the default-layout branch doesn't call triggerViewRefresh,
     only the button's own click handler does). Fixed by reordering init: setupFullscreenAndFilterToggles()
     now runs first (so the side-layout class is settled before any view's own initial fit measures its
     container), then the view setups, then setupNavigator() last (so its tab-activation trigger always
     finds an already-registered hook). Verified on both a landscape (side-layout, 870px container -> 862px
     SVG, no overflow) and a portrait (no side-layout, 641px container -> 633px SVG) viewport, plus
     confirmed the Genealogy tab still renders without errors after the reorder. make test all green.

- Fifteenth follow-up round: fixed a bug you found testing the new relation picker locally -- the picker
  list itself was correct, but the bottom info/note box didn't update to reflect the newly picked
  relation. Root cause: the note box is pinned by a *separate* mechanism from the picker -- each edge
  hit-area path (hp) has its own click listener (attached via createPinnableInfoBox's attach(), from
  before the picker existed) that unconditionally pins the box to *that specific element's* text whenever
  it's the literal DOM click target. Opening the picker doesn't stop the click event from continuing on to
  that target, so on every ambiguous click the box got immediately (and invisibly) pinned to whichever hp
  happened to be topmost in z-order at that pixel -- completely independent of, and never revisited by,
  whatever the user actually picked from the menu afterward. Fixed by: (1) calling ev.stopPropagation() in
  the ambiguous-click branch so the topmost hp's own listener never fires in the first place, and (2)
  exposing a pin(text, pdfEntries) method on createPinnableInfoBox's returned object (previously only
  reachable internally as show()), called explicitly from the picker's onPick callback with the *chosen*
  candidate's own text and PDF entries. Verified with Playwright: picking different items from the same
  8-relation overlap now correctly shows each one's own distinct text (with its own "Open PDF" button)
  in the note box, matching the picker selection every time. make test all green.

- Fourteenth follow-up round: implemented the "overlapping relation lines" TODO item (in the layered
  layout, when ciphers align vertically, one relation line can sit on top of another, making the one
  underneath unselectable by a normal click). Confirmed the interaction model first (click-triggered
  picker, not hover-triggered, so the existing hover-preview behavior stays untouched) before building
  anything. Each edge hit-area path (hp) now carries its own hover-text label in edgeHitData, and
  hitsAtPoint() collects the distinct labeled edges actually hit (edgeCandidates) alongside its existing
  edgeEls/nodeEls union. genPlot's click handler now checks edgeCandidates.length: if there's only one
  (the common case), behavior is unchanged (pin it directly, click again to unpin); if there's more than
  one, a small floating picker (showRelationPicker(), positioned at the click point, clamped to the
  viewport, dismissed by clicking outside it or Escape) lists each one by its own relation text, and
  picking an item pins just that one relation and its two endpoint families (with the same click-same-
  item-again-to-unpin symmetry as a direct click). Verified against real data with Playwright: found an
  actual 8-relation overlap point in the layered layout (NewDES's shared-construction fan-out) where the
  picker correctly lists every distinct relation and picking one pins only that pair; confirmed clicking a
  genuinely unambiguous single relation still pins directly with no picker; confirmed dismiss-by-clicking-
  elsewhere and the picker's own listeners don't fight with the existing pin/unpin document-click handler
  (via stopPropagation on picker items and a deferred outside-click listener so the opening click itself
  doesn't immediately close it). Noted for you: at NewDES's exact busiest hub the sampled point actually
  turned up 54 overlapping candidates (the pointer-tolerance ring picks up more than what's under the
  single center pixel) -- the picker scrolls rather than breaking, but that's a genuinely crowded case
  worth knowing about if a future refinement (e.g. grouping multiple construction-sharing reasons for the
  same src/tgt pair into one entry) turns out to be wanted. make test all green.

- Thirteenth follow-up round: fixed the "find family" relation-degree filter (was in the TODO section as
  "only the predecessors and successor that eventually connect to the cipher should be displayed, not also
  the first or second degree relations of the ciphers that are not the focused cipher"). Root cause: at
  degree N, the old code did one *undirected* BFS N hops out from the matched family, which meant a node 2
  undirected hops away could be a genuine grandchild/grandparent OR a *sibling* reached by walking backward
  to a shared parent and forking the other way (e.g. focused=C, A->B, A->C: at degree 2 the old code showed
  B even though B has no predecessor/successor relationship with C at all, it's A's other child). Confirmed
  the exact failure with a worked example the user gave (A->B, A->C, C->D, D->E, D->F, G->F; focus=C,
  degree=2 should show A,C,D,E,F but not B or G) and reproduced/fixed it with a standalone Node repro before
  touching the real code. Fix: two independent directional BFS chains (ancestors-of-ancestors,
  descendants-of-descendants) unioned with the matched set, instead of one undirected walk -- safe because
  the influence graph is acyclic (test_influences_acyclic.py), so a node can never be both an ancestor and a
  descendant and the two chains never cross. Confirmed by the user that this should apply per matched
  family when the search text matches more than one (e.g. "SKINNY" exact-word-matches both "SKINNY" and
  "SKINNY-AEAD" since the hyphen counts as a word boundary) -- already correct as implemented, since seeding
  both frontiers with the whole matched set at once is mathematically the union of each match's own
  ancestor/descendant chains. Verified against the synthetic example (exact match) and against real data
  (Piccolo -> {SKINNY, Splight}; focusing SKINNY at degree 2 correctly excludes Splight while including
  REMUS via Romulus; focusing Piccolo directly correctly includes Splight, since it's Piccolo's own child,
  not a sibling). make test all green.

- Twelfth follow-up round: you liked the new corner-triangle declutter toggle and asked to move it from
  top-left to top-right on both frames. Moved it (position: top:0;right:0) and mirrored the CSS border
  triangle itself (border-width: 18px 0 0 18px instead of 18px 18px 0 0) so its right-angle vertex still
  points into the actual corner rather than away from it. Checked the one real risk of this move --
  top-right is also where the "Tune layout" button already floats -- via a Playwright screenshot and a
  getBoundingClientRect() overlap check: the two boxes touch by ~7x7px right at Tune layout's own rounded
  corner, which reads as a hairline graze rather than a real visual clash and doesn't cover any text.
  Verified the toggle's click/hide-restore behavior still works (CSS-only change). make test all green.

- Eleventh follow-up round: three small UI polish requests after confirming the hover/pin fixes work.
  1. Removed the "📌 Something is pinned" banner (genPinnedBanner) entirely -- you found it unnecessary
     since pinning/unpinning is already obvious. Collapsed the now-pointless setHotPinned() wrapper back
     into plain `hotPinned = ...` assignments at each call site.
  2. Increased the hover/pin highlight's opacity boost from +0.1 to +0.2 over the base edge opacity
     (layoutParams.edgeOpacity, 0.1 by default) for a more visible highlight.
  3. Replaced the "Hide UI" / "Show UI" text button (which was itself another floating button sitting over
     the plot's top-left corner -- the exact thing it was meant to declutter) with a small folded-corner
     CSS triangle (18px legs, no text) tucked flush into the corner, with the same aria-pressed/aria-label
     toggle behavior; it flips to the opposite diagonal when pressed as a lightweight visual cue. Applied
     to both the Genealogy and Timelines frames, since they share the same toggle.
  Also found and reran a failed "Deploy GitHub Pages" run for the prior debug-cleanup commit (a transient
  "Service Unavailable" resolving a GitHub Action, not a code issue). Rebuilt the site and verified with
  Playwright: no genPinnedBanner anywhere, Tune Layout panel free of debug checkboxes, hover sets
  opacity="0.3" (0.1 + 0.2) on hot edges, click-to-pin/click-elsewhere-to-unpin still work, and the new
  triangle toggle correctly hides/restores the floating controls. make test all green, no console errors.

- Tenth follow-up round: you confirmed the pointer-events:"all" fix actually works live, and asked to
  remove all the debugging-only tooling that had accumulated across this investigation (the three "Show
  edge/node/pointer hit-area (debug)" checkboxes, the pointer hit-area marker, the live debug readout, and
  debugOutlineNodeHitArea()) now that it's no longer needed. Removed all of it from app.js,
  index.html.tmpl, and styles.css (grep for "genDebug", "debugOutlineNodeHitArea", "describeHitEl",
  "updateDebugReadout", "rawHits", and "hitAreaStroke" across all three files now returns nothing), while
  keeping the two permanent fixes those tools were built to verify: pointer-events:"visibleStroke" on the
  edge hit-area paths (with its explanatory comment intact) and the scroll/zoom hover-refresh logic.
  Deliberately kept the "📌 Something is pinned" banner even though it was introduced during this same
  debugging round -- it's a genuine, permanent UX affordance (a pin looks identical to a live hover
  otherwise, so without it a pinned relation can look like a stuck/buggy highlight), not diagnostic
  tooling, so it wasn't in scope for removal. Rebuilt the site and verified with Playwright against the
  rebuilt static site: hovering an on-curve point highlights correctly, clicking pins it (banner appears)
  and the highlight survives moving the mouse far away, clicking empty space unpins it (banner
  disappears), the straight-chord interior-hit-testing bug stays fixed (0 hits at the chord midpoint of
  the most-bulging curve), and there are zero console errors. make test all green.

- Ninth follow-up round: ACTUAL root cause of the whole "cursor is over visible white space, but a
  relation is highlighted" saga, found because you correctly guessed the shape of the bug from watching
  it happen -- the area between a curved relation line and the straight ("shortest") line connecting its
  two endpoints was the thing getting hit-tested. Confirmed exactly right: every edge hit-area path (hp)
  was created with pointer-events:"all", which -- despite fill:"none" -- still hit-tests the path's
  geometric *interior*, computed by implicitly closing the (open, unclosed) curve back to its start with
  a straight line. A wide S-curve bulging far from that implicit closing line had its *entire enclosed
  area* hit-testable, not just the stroked line itself. Confirmed directly: a point sitting on the
  straight chord between an edge's two endpoints, ~334 viewBox units from the actual visible curve,
  registered as a hit before the fix and correctly stopped after it. This also fully explains the
  "52 edges hot at a hub, only 17 really there" measurement from the scroll-staleness round -- re-ran
  the identical hub hover after this fix and got 2 genuine hits instead of 52, meaning most of that
  discrepancy was never staleness at all, it was this. Fixed by using pointer-events:"visibleStroke"
  instead of "all" for the hp paths (fill:"none" alone does not opt out of the interior-hit-testing
  behavor -- pointer-events has to). Verified the fix doesn't break genuine on-curve interaction:
  hovering/clicking an exact point on a curve's stroke still fires the native tooltip, highlights, and
  pins correctly. make test and a full build-site pass; no console errors.

- Eighth follow-up round: still saw "cursor over visible white space, but a relation/label highlights"
  after the scroll/zoom staleness fix. Ruled out a stale pin (you confirmed no click happened) and ruled
  out eyeballing a screenshot as a reliable way to verify the node-hit-area debug outlines actually line
  up with what hitsAtPoint() finds, so built the two things that make this directly, unambiguously
  checkable instead of guessable from an image:
  1. A live debug readout (bottom-left of the frame, shown with "Show pointer hit-area (debug)") that
     lists in real time exactly what hitsAtPoint() finds at the current cursor position -- edge/node
     counts plus each match's actual label text (e.g. "Poly1305 -> AES-GCM: shares construction..."),
     not just a screenshot of colored outlines that still requires guessing what they refer to.
  2. A "📌 Something is pinned" banner shown whenever hotPinned is set, regardless of where the cursor
     currently is -- a pin looks identical to a fresh hover otherwise, which is easy to mistake for a
     stuck/buggy highlight if you don't realize something was pinned earlier (ruled out for the specific
     report here, but worth having permanently either way).
  make test and a full build-site pass; no console errors. Next step is for you to check the live
  readout yourself against what you're seeing, since I can no longer usefully speculate further from a
  screenshot alone.

- Seventh follow-up round: added a second debug checkbox, "Show pointer hit-area (debug)", next to the
  existing edge-hit-area one -- draws the actual small tolerance ring hitsAtPoint() samples around the
  cursor (a plain fixed-position HTML circle, since the sampling itself works in screen/client pixels,
  not the plot's viewBox units, so no coordinate conversion is needed for it to line up exactly).
  First hypothesis on the report (cursor visually in white space, several relations highlighted, no
  click involved) was wrong -- guessed low edge opacity (10%) was making genuine nearby lines invisible.
  You followed up with a zoomed screenshot showing the cursor marker in *clearly* empty space with no
  hit-area lines anywhere near it, which ruled that out and pointed at something structural instead.
  ACTUAL cause, confirmed by direct measurement: scrolling/panning genPlotScroll, or zooming (the
  toolbar +/-/Fit/Reset buttons especially, since unlike Ctrl+wheel they don't re-center on the cursor)
  moves plot content underneath a cursor that never fired its own mousemove -- nothing re-checked what
  was actually there afterward. Reproduced directly: hover a dense hub (52 edges), pan the container a
  few hundred px with the mouse held still, and the *same* 52 edges stayed classed hot while a fresh
  hit-test at that exact screen position found only 17 actually there anymore. Fixed by tracking the
  last known pointer position and re-running the same hit-test from genPlotScroll's scroll handler and
  from applyGenZoom() (the common path for every zoom operation) -- confirmed the stale-vs-actual counts
  now match (19 vs 17) after the same pan, and similarly after zooming via both Ctrl+wheel and the
  toolbar buttons. make test and a full build-site pass; no console errors.

- Sixth follow-up round: clarified that hover-precision tolerance belongs on the *pointer*, not the line
  -- an edge's hit-area should match its visible width exactly, with a small extra allowance sampled
  around the actual cursor position instead. Implemented: hp path stroke-width is now exactly the edge's
  own thickest visible stroke (no added tolerance at all), and hitsAtPoint() now checks a ring of 6 sample
  points at a 3px screen-pixel radius around the cursor (elementsFromPoint() at each, deduped by element)
  in addition to the exact cursor position. This also fixes a real inconsistency the old approach had:
  a viewBox-unit-based line-fattening tolerance scales with zoom (ballooning at high zoom, shrinking at
  low zoom), where a screen-pixel-based pointer radius stays a constant, predictable size regardless of
  zoom level. Verified via the hit-area debug view (visibly thinner, tracking each line's own width) and
  by checking a 2px cursor offset still hits the same edge while a 30px offset doesn't. make test and a
  full build-site pass; no console errors.

- Fifth follow-up round, reported directly rather than filed here first: you asked to *see* the edge
  hit-areas to investigate "many relations stay highlighted after the cursor passed and left."
  1. Added a permanent "Show hit-areas (debug)" checkbox to Genealogy's Tune Layout panel (draws each
     edge's actual invisible hit-area in solid red) rather than a one-off screenshot, so this is
     inspectable directly whenever it's needed again. Confirmed via it that the hit-areas themselves are
     now appropriately thin (previous round's fix) -- what looks like "many highlighted" right at a hub
     is genuinely dozens of distinct curves passing through the same few pixels there, not oversized
     hit-areas.
  2. The actual "stuck after leaving" cause: tested pure hover-through-then-away (no click) with real
     synthetic pointer movement and it correctly cleared every time -- the behavior you hit is the pin
     mechanism (click to pin, added two rounds ago) triggered unintentionally, most likely by a trackpad
     tap while passing through a dense hub. Once pinned, there was no way to dismiss it except clicking
     the exact same spot again, which is easy to lose track of on a hub with dozens of edges. Added two
     "click away to dismiss" escape hatches: clicking empty space *inside* the plot, and (a separate fix,
     since a click outside genPlot's own SVG never reaches a listener registered on it at all) clicking
     *anywhere else on the page* while something's pinned.
  Verified via Playwright with real (not synthetic-dispatch) pointer movement for the hover/leave check,
  and explicit pin-then-click-elsewhere sequences for both new dismiss paths. make test and a full
  build-site pass; no console errors.

- Fourth follow-up round, reported directly rather than filed here first:
  1. Edge hover hit-area still too generous after the previous round's fix (tracking visible thickness +3
     units of tolerance) -- reduced the tolerance to +0.5, close to "a dot" as requested.
  2. Genealogy/Timelines: the floating overlay buttons (Hide filters/Filters on top/Exit fullscreen in
     fullscreen; Tune Layout always; the -/+/Fit zoom controls always) sit in the plot's corners and can
     cover family labels there, especially in layered mode. Added a small semi-transparent "Hide UI"
     toggle in the one corner none of them occupy (top-left, never itself hidden) that hides every other
     floating control at once; click again ("Show UI") to bring them back. Chose an explicit click toggle
     over a hover-to-reveal one specifically because hover-to-reveal would have made labels underneath the
     buttons unselectable exactly when you'd want to reach them.
  Verified via Playwright: the hide toggle correctly hides/restores all three control groups in both
  fullscreen and normal mode on both tabs; edge hover still finds edges correctly at the tighter hit-area
  width. make test and a full build-site pass; no console errors.

- Third follow-up round, reported directly rather than filed here first:
  1. Timelines axis, ACTUAL root cause finally found (all previous fixes were treating symptoms): you
     suggested directly measuring the screen-pixel distance between consecutive axis ticks vs consecutive
     plot gridlines, which exposed it immediately. xAxisSvg's height (AXIS_HEIGHT) never scales with zoom
     while its width does, and yAxisSvg's width (LEFT_AXIS_WIDTH) never scales while its height does -- so
     at any zoom other than 1x, each axis SVG's CSS box has a *different aspect ratio than its own
     viewBox*. SVG's default preserveAspectRatio ("xMidYMid meet") reacts to that by uniformly scaling to
     whichever dimension needs the *smaller* factor and letterboxing the rest -- confirmed via
     getScreenCTM() at a large fit-to-width zoom (reachable on a wide monitor): xAxisSvg's actual on-screen
     scale was exactly 1.0 (its unscaled-height constraint) while plotSvg's was the intended ~1.34x, so the
     ticks rendered compressed and centered instead of stretched to fill the box -- a genuine ~1.5x scale
     mismatch, not a sub-pixel rounding issue at all. plotSvg itself never showed this since both its own
     dimensions scale together, keeping its aspect ratio matched to its viewBox at any zoom -- which is
     exactly why only the axes drifted and why it was so hard to pin down. Fixed with
     preserveAspectRatio="none" on all three Timelines SVGs (stretch to exactly fill the box on both axes,
     which the rest of the code already assumed happened). Re-verified with the same tick-vs-gridline
     screen-position measurement at an exaggerated wide+scaled-display viewport (3200px, devicePixelRatio
     1.25): before the fix, consecutive ticks were spaced ~1.5x wider on screen than consecutive gridlines;
     after, their absolute screen positions match exactly. Also switched the axis's horizontal scroll sync
     from a CSS translateX() transform to native scrollLeft assignment on the axis pane itself, so both
     plot and axis share the same scroll-positioning code path instead of two that could in principle
     round differently.
  2. Edge hover precision: the invisible hit-area under each edge was far fatter than the edge's own
     visible line (a flat "at least 10 units wide" minimum, vs. lines as thin as 0.8-3.4 units) --
     combined with the union-highlighting from the previous round, that meant hovering almost anywhere
     near a cluster of close-but-distinct lines matched several of them together, making it hard to target
     one specifically. Changed the hit-area width to track each edge's own visible thickness (+3 units of
     tolerance) instead of a fixed generous minimum.
  Verified via Playwright with getScreenCTM()-based screen-position measurements (not just DOM attribute
  comparisons, which would have missed this entirely -- the underlying viewBox-unit coordinates were
  already correct and identical, this was purely a screen-rendering-time scaling bug). make test and a
  full build-site pass; no console errors.

- Second follow-up round, reported directly rather than filed here first:
  1. Timelines axis, root cause finally identified: you narrowed it to moving the browser window from a
     laptop's screen to a wide external monitor -- a different-DPI monitor switch, which commonly keeps
     the window's *logical* size the same (no `resize` event fires) while changing devicePixelRatio, and
     also raised a real bug in my own previous ResizeObserver fix: it re-measured plotSvg's width via
     getBoundingClientRect() (a layout-precision float that can differ from the exact CSS text applyZoom()
     set, once device-pixel rounding is involved) and copied that float onto xAxisSvg, which at the very
     large fit-to-width zoom multiplier a wide monitor produces amplified a fractional-pixel difference
     into a visible drift toward the right edge. Fixed by copying plotSvg.style.width's exact string
     instead of re-measuring, and added a devicePixelRatio-change watcher (the standard matchMedia-based
     technique, since there's no native event for it) that re-fits both Timelines and Genealogy whenever
     the effective DPI changes, resize event or not.
  2. Reworked the pin/hover interaction again: hovering something else while a relation is pinned now
     shows *both* highlighted at once (a union), instead of the hover replacing the pin -- so the pin
     genuinely only goes away via a second click on the same edge/node, never by hovering or moving the
     mouse away, while hover previews still work throughout.
  3. Highlighted labels: bold text alone didn't read well against the busy plot background. Replaced it
     with a small opaque backdrop rect (fixed high-contrast color pair, not derived from the legend)
     inserted directly behind the label, positioned via getBBox() and mirroring the label's own rotation
     transform for radial mode.
  4. Highlighted-edge opacity was forced to 100%, which buried labels under a wall of opaque lines on
     dense graphs. Changed to current opacity (the Edge opacity slider's value) + 10 percentage points,
     capped at 100% -- the extra stroke-width already does most of the "this one's highlighted" work, so
     full opacity was never actually necessary.
  Also answered (no code change): construction_sharing_edges is the *stored output* of the automatic
  derivation rule (compute_construction_sharing_edges), not separate hand-curated data it duplicates --
  actively read by Genealogy for the shared-construction star-topology edges, kept as-is per your call.
  Verified via Playwright (fixed-coordinate synthetic events, since a real mouse hover over a dense
  overlapping-edge point isn't reproducible otherwise -- careful this time not to scroll the container
  between measurements, which produced a false "unpin didn't work" result during testing). make test and
  a full build-site pass; no console errors.

- Follow-up round on the Timelines/Genealogy work above, reported directly rather than filed here first:
  1. Timelines axis, still misaligned after the offsetWidth fix: added a ResizeObserver that mirrors
     xAxisSvg's width directly off plotSvg's actual rendered width, so they can never visibly disagree
     regardless of the exact root cause (belt-and-suspenders on top of the existing fit fix).
  2. Genealogy default edge opacity lowered from 50% to 10% -- even 50% buried labels on large plots.
  3. Reworked the hover/pin interaction entirely: hovering now keeps working after clicking to pin
     something (previously a pin blocked all further hover updates); moving the mouse off the plot falls
     back to showing the pinned edge instead of clearing the highlight.
  4. Replaced per-element mouseenter/mouseleave/click listeners with a single delegated mousemove/click
     pair on genPlot using elementsFromPoint(). With 1000+ overlapping invisible edge hit-areas, native
     mouseenter only ever reached whichever one was topmost in paint order -- anything underneath was
     silently, permanently unreachable ("some relations never highlightable"). elementsFromPoint()
     collects every hit-area actually under the cursor, so overlapping edges highlight/pin together.
  5. Node hover/click wasn't working at all: nodes now register into the same hit-test map as edges
     (radial labels are also now always hit-testable, not just when there's no bullet shown), so
     hovering/clicking a node highlights itself, every edge touching it, and those edges' other
     endpoints -- mirroring what edge-hover already did.
  Verified via Playwright (direct DOM event dispatch with elements scrolled into view first, since a
  real mouse hover over one of ~1400 overlapping invisible hit-areas isn't reproducible via synthetic
  pointer coordinates otherwise): hover-after-pin, mouseleave-falls-back-to-pinned, buried-edge
  union-highlighting, and node-hover all confirmed working in both layouts. make test and a full
  build-site pass; no console errors.

- Reference PDF filename audit: fixed the specific files flagged (see conversation) plus a systematic
  sweep for the same two problems elsewhere:
  1. Wrong names (a name in the filename that the document doesn't actually back): the Skein/Threefish
     spec had helix/helix_aead/phelix/phelix_aead in its names field -- those come from Helix/Phelix
     merely being *cited* in the spec's own bibliography (as the source of its MIX function), not from
     the spec documenting them; both already have their own dedicated files. Renamed to
     [2010][skein,threefish][][].pdf. Checked the other 4+-name files for the same mistake
     (bear_lion,cast256,mars,rc6,skipjack; ade,kalyna,mukhomor,rsb; the SHA/SHACAL FIPS; the Ascon NIST
     SP) -- all confirmed legitimate on inspection, no further fix needed.
  2. Uninformative/redundant notes (a notes field that just restates part of the names field in a
     different separator, adding no information): fixed the ~10 explicitly flagged files plus ~20 more
     found via the same pattern (remus/romulus, locus/lotus/twegift_64, drygascon, elephant, gage,
     cast128, cast256, cs_cipher, lili_128, sha1_hash, hc_128, katan/ktantan, simon/speck, joltik_bc,
     cube_cipher, scream_tbc, mantis/skinny, friday_hash/jarvis, isap/simple/spix/spoc_aead,
     yarara_and_coral, cham_revised, safer_plus/saferpp). A few looked similar but turned out to add
     real information beyond the names field and were deliberately left alone (md2's "rsamd2" identifies
     the RSA-authored source; bear_lion's note adds "lioness", not in names; feal's "fealnx" and snow's
     "snow10" disambiguate from sibling files; armadillo/curupira's notes distinguish multiple dated
     papers; rasta's "agrasta" and grain_128's "grain128aead" flag related designs not yet modeled as
     their own families). Also: hadesmimc's two single-name files got clarifying notes instead of
     removal (hades_design_strategy / starkad_original_eprint) since 3 hadesmimc-related files exist and
     genuinely need telling apart; snow_3g's "spec" note moved into the doctype field, matching the
     existing convention used by 2 other files; the two ChaCha20+Poly1305 RFC files got their shared,
     non-distinguishing "chacha20" note replaced with their actual RFC numbers (rfc7539/rfc8439).
  reference_pdfs.py, make test, and a full build-site pass confirm every renamed family still resolves
  (unresolved count unchanged at 82, unparsed filenames at 0).

- Genealogy relation-edge follow-ups, reported directly rather than filed here first:
  1. Removed "Show relation arrows" from Timelines' plot controls, along with the drawing code behind it
     (relationEndpointPairs, relationInfo, the arrowhead marker, the edge/hover-line layer) -- relation
     edges are Genealogy's job now, and this duplicated a "fit endpoints from rendered label bounds"
     path no other Timelines feature needed.
  2. Added an "Edge opacity" slider to Genealogy's Tune Layout panel (both layouts, plus collapsed-edges
     mode), persisted/exported/imported alongside the existing spacing sliders. Defaults to 50%, matching
     the opacity that was already hardcoded -- edges default to less than fully opaque since dense graphs
     otherwise bury family labels under overlapping lines.
  3. Hovering (or clicking, which pins it through the next hover -- click the same edge again to unpin)
     a relation edge's hit-area now bolds that edge to full opacity and a thicker stroke, and highlights
     its two endpoint nodes (a bright stroke ring on the node box/bullet, bold label text), in both the
     radial and layered layouts. Implemented as two small shared helpers (setHotHighlight/
     clearHotHighlight) plus a per-node-id element map populated by each layout's own node-drawing loop.
  Verified via Playwright (direct DOM event dispatch, since a real mouse hover over one of ~1400
  overlapping invisible hit-areas isn't reliably reproducible via synthetic pointer coordinates):
  hover/click/unpin all toggle the right classes in both layouts and in collapsed-edges mode; the
  opacity slider's value is reflected on the actual edge paths. make test and a full build-site pass;
  no console errors.

- Three fullscreen UI bugs (Timelines + Genealogy tabs), reported directly rather than filed here first:
  1. Timelines fullscreen: the years axis didn't stretch to match the plot width, losing alignment with
     the vertical gridlines. First fix (incomplete): nothing listened for a native window `resize` event
     while a panel was in our CSS-overlay "fullscreen" mode, so the browser's real fullscreen (F11) or
     resizing/maximizing the window *after* toggling it never re-triggered the fit -- added a debounced
     window resize listener for that case. This didn't fix the case you actually hit, though (no resize
     needed, happens on the very first fullscreen toggle): on a browser with classic, space-reserving
     scrollbars (not macOS-style overlay ones), a tall plot gives vizPlotScroll its own vertical
     scrollbar, shrinking its clientWidth below the x-axis pane's (which never scrolls vertically since
     it's a fixed 48px-tall strip) -- fitting *both* panes' width to plotScroll.clientWidth left the axis
     short of the plot's actual right edge every time, independent of any resize. Fixed by fitting to
     offsetWidth instead (the full grid-column width, unreduced by the pane's own scrollbar), which both
     panes share since they're the same CSS grid column.
  2. Genealogy fullscreen: the plot didn't stretch all the way down, leaving a beige gap at the bottom.
     Root cause: genFrame's height is fit to window.innerHeight with a fullscreen-vs-normal ratio, but
     that calculation only ran inside the full layout functions (drawSugiyama/drawRadial), not inside
     the zoom-refit path (ensureGenFit/applyGenZoom) that the Fullscreen button actually triggers -- so
     toggling fullscreen resized the plot's zoom but never grew the frame around it. Timelines already
     got this right (vizFrame's height is recomputed inside applyZoom every time); ported the same
     pattern to Genealogy via a shared recomputeGenFrameHeight() call from applyGenZoom, drawSugiyama,
     and drawRadial.
  3. Both tabs: the "Tune layout" button overlapped "Exit fullscreen" (both float in the same top-right
     corner in fullscreen -- .view-header-row at top:0.6rem/right:0.6rem, .gen-params-toggle at
     top:0.7rem/right:0.7rem -- and the toggle's higher z-index put it in front, intercepting clicks
     meant for Exit fullscreen). Pushed .gen-params-toggle/.gen-params-panel down below the header row
     specifically in fullscreen mode.
  Verified via Playwright: no bounding-box overlap on either tab, a real (non-force) click on Exit
  fullscreen now works, axis/plot widths match after a simulated window resize while fullscreen, and
  genFrame's height now fills ~97% of viewport height instead of staying stuck at its pre-fullscreen
  size. make test and a full build-site pass; no console errors.

- add N-hash https://en.wikipedia.org/wiki/N-hash
  add new construction type described by the wikipedia page
  Added: no official spec is online, so modeled directly from the primary source you provided
  (references/1990-n-hash-extract_p12-16.pdf, Biham & Shamir, "Differential Cryptanalysis of Feal
  and N-Hash," EUROCRYPT '91) rather than the earlier secondary summary -- the paper's Figures 2-4
  confirm the compression function precisely: f(h,m) = g(h,m) XOR m XOR h, textually described as
  "the XOR of the output of the g-function with the block itself and with the old hashed value."
  That's a Miyaguchi-Preneel-shaped double feedforward (both message and chaining value XORed in),
  not Davies-Meyer or Matyas-Meyer-Oseas, so no unconfirmed-formula flag was needed.
  No new construction type was needed either: g's internal PS round (Figure 3-4, "F similar to
  Feal's" called 4x per round over four 32-bit words in two layers of two parallel F-calls) matches
  the already-existing Type-2 Generalized Feistel Network taxonomy, just doubled up within one PS
  round -- so it's tagged feistel/type2_generalized_feistel alongside sbl_block_cipher_compression/
  miyaguchi_preneel, plus inspired_by feal (the F-function is FEAL's S-box arithmetic, extended
  from 2 to 4 words). Added as a primitive-tier compression_function family (n_hash) plus a
  mode-tier hash family (n_hash_hash), following the same MD4/MD4-hash split pattern; added
  n_hash_round; added references biham_shamir_1991_feal_nhash (local copy renamed to
  references/[1991][n_hash,n_hash_hash][][biham_shamir_differential_cryptanalysis].pdf) and
  miyaguchi_ohta_iwata_1990_nhash (the original SECURICOM '90 spec, no local copy -- cited
  secondhand only, per the paper's own bibliography).

- process_stages table has a parent_stage_id column which seems to always be empty. can it be
  removed?
  Removed: confirmed 0 uses across data/processes.yaml and no downstream reader (export_viz.py,
  app.js); dropped from schema/processes.schema.json and scripts/build_db.py.

- are these properties needed in the mode properties: block_size_bits, state_size_bits, rounds?
  Yes, keep all three -- checked actual usage: state_size_bits appears on 87 families/193
  instances, block_size_bits on 17 families/78 instances, and rounds on 83 instances (family-level
  rounds is unused, but that's fine, it's optional and varies per instance/variant).

- Duplex mode should be duplex construction, similarly to Sponge construction. Or they should be
  at least consistent.
  Renamed duplex_mode -> duplex_construction (id and name) across mode_constructions.yaml and all
  22 referencing family entries in mode_families.yaml.

- is DrySponge a duplex or a sponge?
  It's a duplex: dry_sponge_duplex is special_case_of duplex_construction (see
  mode_constructions.yaml) -- this was already modeled, just never checked off.

- Not touched, minor: primitive_constructions.yaml's typical_primitive_types field lists
  stream_cipher even though it isn't a primitive type (it's mode-tier).
  Replaced stream_cipher with update_function on all 16 affected leaves -- every one of them
  describes a state-update mechanism, which is exactly what update_function means.

- expand incomplete construction entries, e.g. filtered_fcsr, filtered_lfsr
  Already fully expanded (this must have been done in an earlier pass and never checked off) --
  both have complete notes and typical_primitive_types in primitive_constructions.yaml.

- create clear definition of component/round/primitive/construction/mode/etc., incl. a
  Merkle-Damgard worked example
  Already substantially covered by GLOSSARY.md (Tiers, Family vs. instance, Types, Constructions,
  Components, Rounds sections) plus README.md's construction-taxonomy section; the
  MD2-is-not-Merkle-Damgard nuance from this TODO is already captured in the MD2 family entry
  (see the MD2/MD4/MD5/HAVAL reference fix further down this DONE list).

- add TWEAKEY framework among the constructions: Tweaks and keys for block ciphers: The TWEAKEY
  framework. ASIACRYPT 2014
  Added as a key/tweak-handling facet (primitive_constructions.yaml, id tweakey_framework), tagged
  onto SKINNY, Deoxys, Joltik-BC, and MANTIS alongside each one's own round-structure root.

- Add PX-structure as a construction (uBlock paper, Pshufb-Xor SIMD linear layer)
  Added (primitive_constructions.yaml, id px_structure) and retagged uBlock with it, replacing the
  more generic bit_permutation_linear_layer tag it carried before.

- are these linear transformations used in hight: F0(x) = x≪1 ⊕ x≪2 ⊕ x≪7, F1(x) = x≪3 ⊕ x≪4 ⊕
  x≪6. similar to what is used in ascon, keccak or xoodoo linear transformations?
  Confirmed exact against the CHES 2006 paper / IETF draft-kisa-hight-00. Same general shape as
  Ascon's per-word linear layer (word XORed with two rotated copies of itself); unlike
  Keccak/Xoodoo's theta step, which mixes across lanes via column parity rather than rotating one
  word in isolation. No citation/authorship link found, so recorded as a structural resemblance
  only. See data/rounds.yaml's hight_round entry.

- look for type 1/2 generalized feistel networks
  Already fully modeled: type1_generalized_feistel through type5_generalized_feistel all exist in
  primitive_constructions.yaml, each citing Zheng/Matsumoto/Imai (CRYPTO '89) and Hoang/Rogaway
  (CRYPTO 2010) with their defining structure and a running-example family.

- add recursive Feistel as a construction
  Already exists: nested_feistel_network ("Nested (Recursive) Feistel Network", MISTY1/KASUMI).

- add universal hash functions
  Already modeled: universal_hash / polynomial_universal_hash / multiply_add_universal_hash
  constructions, plus GHASH, Poly1305, and UMAC as full mode_families entries. VMAC is the one
  still-missing family (already flagged in UMAC's own note) -- didn't add it since it needs a
  primary-source reference (RFC/paper) I didn't want to fabricate.

- add algebraic/arithmetization oriented ciphers
  Already modeled: LowMC, MiMC, GMiMC, HadesMiMC, Poseidon, Rescue, and Vision all exist as
  families, plus the hades/algebraic_feistel constructions. Anemoi/Griffin (newer, 2022-2023) are
  not yet added -- not what was asked here, but flagging in case you want them too.

- 4 genuine gaps where the yaml reference entry had no URL at all:
  alvarez_et_al_1996_akelarre (Akelarre),
  leech_1996_crisp (CRISP),
  cs_cipher_ref (CS-Cipher), and
  bmgl_ref (BMGL).
  I added the checklist's URLs to those entries in data/references.yaml (validated the YAML still parses).

Which is the first AndRX cipher? Simon for now

- ciphers to add:
  ZORRO (together with PICARO and earlier NOEKEON, designed with Side-Channel protection in mind)
  Joltic-BC, SCREAM internal cipher -> leakage resilient design with extra tweak input

- In the symmetric cryptography database: add BLAKE3
  include relations with its predecessors

- In the symmetric cryptography database: WARP (i already added the pdf [2020][warp][][].pdf)

- In the symmetric cryptography database: FORRO (i already added the pdf [2022][forro][][].pdf)
  include relations with its predecessors


- In the symmetric cryptography database:
  Fix the references for
  MD2: Published in 1989 by Ronald Rivest as a checksum algorithm, which was later adopted for Public Key Infrastructures.
       Added [1989][md2][rfc][rsamd2].txt, specs as source code in Section 4.2.
       Not a proper Merkle–Damgård construction.
  MD4: Published in 1990 at the CRYPTO conference. It was created by Rivest as a much faster alternative to MD2.
  MD5: Published in 1992 as IETF RFC 1321, replacing MD4 after potential security weaknesses were identified in the older algorithm.
  HAVAL: Introduced in 1992 at the AUSCRYPT conference by Yuliang Zheng, Josef Pieprzyk, and Jennifer Seberry.
  Note: I have added the two references:
  symmetric_cryptography_database/references/[1989][md2][rfc][rsamd2].txt
  symmetric_cryptography_database/references/[1990][md4,md4_hash][][crypto].pdf

- Move the About tab to be the left most, followed by Glossary, then add a "Disclaimer" tab; group the
  tabs into Views / Tables / Documentation.
  Grouped the nav bar into three labeled clusters -- Views (Timelines, Genealogy), Tables (All SQLite
  Tables, Custom Query Builder), Documentation (About, Glossary, Disclaimer, in that order). Asked and
  confirmed: Documentation sits last (after the two working-tab groups), not literally first -- the
  grouping idea superseded the original "About leftmost" phrasing. Added the Disclaimer tab with the
  requested AI-crawl/manual-review-pending text. (index.html.tmpl nav markup + new .nav-group/.nav-group-label
  styles.css, new Disclaimer view-panel.)

- Propose other interesting visualizations to be added in the website.
  See the new "Proposed visualizations" entries added at the top of the TODO section above (construction
  treemap, adoption-over-time chart, parameter-space scatter, process attrition funnel, component usage
  heatmap, round-signature similarity view) -- none implemented yet, just proposed as requested.

- set the following defaults: panels on the left enabled when on a large landscape screen (e.g. a laptop).
  Default via window.matchMedia('(min-width: 1024px) and (orientation: landscape)') for Timelines/
  Genealogy/Query Builder at page load; portrait/narrow screens keep the stacked-on-top default. The
  existing manual toggle still overrides it for the rest of the session. (app.js,
  setupFullscreenAndFilterToggles)

- When from radial the user clicks layers, or viceversa, make sure that the plot starts from the Fit
  position.
  Genealogy's Layered/Radial buttons now call fitGenZoom() right after switching, so the plot always
  starts from Fit instead of carrying over whatever zoom/pan was set in the previous layout.

- Reuse as much code as possible for the filters of the Tables and of the Views.
  Reused the Views' filter UX in the Custom Query Builder tab: added the Filters-on-left/top toggle and
  Collapse all/Expand all buttons (the same generic data-toggle-layout/data-collapse-all wiring
  Timelines/Genealogy already use, no new JS mechanism), and replaced the free-typed Reference Year
  Min/Max number inputs with the same From/To year range-slider component Timelines/Genealogy use.
  "All SQLite Tables" intentionally left alone -- it's a raw per-table browser (table picker + cell
  search) with no family/relation concept to filter on, unlike the Query Builder, so there is no
  equivalent filter UI to reuse there.

- It does not make sense to color by application or construction (most ciphers have several of each);
  maybe highlight a chosen set instead.
  Removed Construction and Target application from Genealogy's "Color nodes by" (kept Type and Process,
  both close to single-valued per family) since coloring by a multi-valued dimension just showed the
  alphabetically-first value. Added a "Highlight" control instead: pick Construction or Target
  application, then check specific values to fade every non-matching node/label to 15% opacity in both
  Radial and Layered layouts, without hiding them outright.

- add Tune Layout button also in the Timeline tab, reusing the same code.
  Added a floating "Tune layout" button/panel to the Timelines tab, reusing the Genealogy panel's exact
  CSS classes/interaction pattern (open/close/reset). Exposes Row spacing and Group gap sliders -- the
  previously-hardcoded STACK_STEP/GROUP_GAP_UNITS constants -- persisted to localStorage the same way
  Genealogy's layout params are.

- In the Timelines tab, add the option None to "Group families by" (labels collapse into a histogram).
  Added: all families collapse into a single "All families" row, so same-year points stack into a
  histogram-like column via the existing per-group stacking logic (no new rendering path needed).

- Fix "Find family" behaviour (exact word vs. contains, e.g. "DES" matching "HADES").
  Added an "Exact word" checkbox next to "Find family" on both Timelines and Genealogy. Off (default) is
  the old substring search; On requires a word-boundary-bounded whole-word match, so "DES" no longer also
  matches "HADES". Shared via a new familyNameMatches() helper.

- the construction_sharing_edges table seems to generate a misleading pattern (long descendant chains).
  BUG-FIXED. compute_construction_sharing_edges (scripts/common.py) built a chronological chain per
  construction leaf (each family linked only to its nearest-earlier neighbor), producing the long columns
  flagged (newdes -> safer -> shark -> square -> ...). Changed to a star: the single earliest-year family
  per leaf is credited as the origin, and every later family sharing that leaf links directly to it, e.g.
  full_sbox_layer is now newdes -> {aes, safer, shark, square, serpent, prince, gift, ...} (one hop deep)
  instead of a 70-long chain. Kept the existing cycle-guard (skip an edge if the target can already reach
  the hub through curated/derived edges). validate.py and make test (incl. test_influences_acyclic.py,
  test_derived_construction_relations.py) pass; rebuilt the db and confirmed the star shape directly.

- process_stage_participants: each row has either family_id or name, rarely both -- bug or intentional?
  PARTIAL-GAP, fixed. Intentional split confirmed: family_id is used when the participant has a full
  family entry; bare name is for competition entrants never modeled as a family (mostly early-eliminated
  eSTREAM/NESSIE/PHC submissions -- 156 legitimately name-only after the fix). But 13 rows used name-only
  for participants that DO have a matching family (bmgl, mir_1, wg, ascon x4, catfish, m3lcrypt,
  pufferfish x2, remus) -- a real backfill gap. Backfilled family_id in data/processes.yaml and added the
  missing process_participations entries (cross-checked against sibling entries) in data/mode_families.yaml
  and data/primitive_families.yaml (ascon gained a second entry, for CAESAR use-case-1 portfolio).
  family_id-only now 335 rows (was 326), name-only 156 (was 169). test_process_alignment.py and full
  make test pass.

- in family_influences, only tea/salsa20/rijndael/prince/present have innovative_idea_ids_json -- bug?
  NOT-A-BUG. innovative_idea_ids on an influence edge is a validated pointer into the *source* family's
  own innovative_ideas list, pinpointing which specific documented idea propagated through that specific
  edge -- a narrower, more precise claim than the influence edge itself. 74 families have innovative_ideas
  catalogued, but only 5 edges cite one via innovative_idea_ids; the rest of the ~988 influence edges are
  general relation claims (inspired_by/variant_of/etc.) not yet pinned to a specific idea. Incremental
  curation, same evidentiary bar as the influence-edges backlog -- not something to bulk-fill on a guess.

- in families, only 8 mode families (chacha20_poly1305, aes_cbc, aes_ctr, hmac_sha256, umac, bmgl,
  aes_gcm, pbkdf2) have authors_json -- bug?
  NOT-A-BUG (structural). authors only exists in mode_families.schema.json, not
  primitive_families.schema.json -- primitive ciphers get authorship from their reference paper's
  authors_json directly. Within mode_families.yaml (232 families) all 8 checked cases are legitimate
  disambiguation, recording the true original designers where they differ from the cited standard
  document's author (e.g. aes_cbc -> Ehrsam/Meyer/Smith/Tuchman 1976 vs. reference author Dworkin's NIST
  SP 800-38A; umac -> the UMAC paper authors vs. RFC 4418's editor Krovetz). Not populated elsewhere
  because most modes' reference authors already equal the true designers, so the field is only added when
  disambiguation is needed -- no evidence of a script/backfill bug.

- add [low priority] Espresso (2015/2017 5G stream cipher proposal with near-zero independent citation
  and known cryptanalysis) as a family, flagging the citation/weakness facts from the research already
  done in this file.
  Added as a mode-tier family (data/mode_families.yaml, id "espresso") -- "stream cipher" is a mode
  category in this database's tiering (same as Trivium/Grain/Rabbit), not a primitive, despite the
  original ask saying primitive_families.yaml. 256-bit Galois-NLFSR + 20-variable/degree-6 Boolean filter,
  128-bit key, 96-bit IV (data/rounds.yaml, id "espresso_round", reusing existing nlfsr/and/xor/
  boolean_filter component vocabulary, no new components/constructions needed). Notes capture: ~73
  Semantic Scholar citations vs. the publisher's own near-zero metric; Yao & Parampalli's 2019/2021
  Galois-to-Fibonacci-LFSR transformation result ("this kind of design should be avoided in the future",
  quoted from the arXiv preprint) breaking it via algebraic and Rønjom-Helleseth attacks; a second,
  independent LSTM/CNN keystream-prediction result (Akhilesh, Singh, Mishra 2024); and a caveat that the
  "for 5G" framing is the authors' own pitch, not evidence of independent adoption. No influence edge
  added -- the paper's related-work section was paywalled/inaccessible, so no "based on"/"derived from"
  language could be confirmed against another family already in the database. Three new references.yaml
  entries added. validate.py and make test pass (487 families, no regressions).

- add the following designs: Pholkos (Bossert et al, CT-RSA 2022), Vistrutah (Avanzi et al., IACR ToSC
  2025) -- can easily be turned into a tweakable cipher as well.
  Added both as primitive_families.yaml entries (ids "pholkos", "vistrutah"), each reusing the AES-round
  SPN/full_sbox_layer/mds_linear_layer constructions plus a dedicated round entry (data/rounds.yaml:
  pholkos_round, vistrutah_round) modeling their paired-AES-round step structure. Influence edges added
  with quoted textual evidence: rijndael -> pholkos (generalization_of, "utilizes the AES round function
  ... extends them to build a family of primitives with state and key sizes of 256 and 512 bits"),
  rijndael -> vistrutah and pholkos -> vistrutah (generalization_of/inspired_by, quoting Vistrutah's "new
  Rijndael" framing and its related-work sentence on building on Pholkos/Simpira/Haraka). Vistrutah's
  notes record the paper's own Remark 1 confirming the "can be turned into a tweakable cipher" claim from
  the todo. Two new references.yaml entries (CT-RSA 2022 / ToSC 2025, both with eprint mirrors). Haraka,
  AESQ, Simpira, and ASURA are cited by both papers as direct design ancestors but aren't yet catalogued
  as families here, so no edges could be added to them -- noted in each family's notes rather than
  fabricated. validate.py and make test pass.

- when tapping or clicking on a label or a relation in both the timelines and genealogy view, add an
  "Open PDF" button inside the note (menu to choose if more than one reference; side-by-side/stacked dual
  viewer for a relation between two families, independently scrollable).
  Added an "Open PDF" button inside the pinned yellow note (both tabs, for a family label/dot AND for a
  relation arrow/edge) whenever a local reference PDF exists for that family. A family note opens a
  single pane; a relation note opens two panes (one per family), defaulting to side-by-side on a
  landscape screen and stacked on portrait, with a button to switch either way. Each pane is an
  independently-scrollable <iframe>, so two pages/figures can be compared without scrolling the page
  underneath; a family with more than one local PDF gets a dropdown to pick which one. Backed by a new
  build-time family_id -> local-PDF-path map (scripts/build_static_site.py, reusing
  scripts/reference_pdfs.py's existing filename-matching logic rather than re-deriving it in JS) baked
  into data.js, plus references/*.pdf|*.txt now get copied into build/site/references/ so the deployed
  site can actually serve/open them.

- when the filters of the Timelines and Genealogy tab are on the left, make Collapse all/Expand all
  static so they're reachable without scrolling back up.
  Made the Collapse all/Expand all bar sticky (position: sticky; top: 0) at the top of the side filter
  column whenever "Filters on left" is active, so it stays reachable after scrolling down through a long
  filter list. Stacked-on-top mode is unaffected (no scroll-away problem there).

- Not touched, minor: primitive_constructions.yaml's typical_primitive_types field
  lists stream_cipher even though it isn't a primitive type (it's mode-tier) — it's a pure documentation field,
  unused by the actual site build, so it doesn't affect the genealogy graph. Happy to clean it up if you want.
  Replaced stream_cipher with update_function on all 16 affected leaves -- every one of them describes a
  state-update mechanism, which is exactly what update_function means.

Other relations (verify them by reading the corresponding pdf):
- SKINNY:
  + follows trend of locally non-optimal internal components: who started it?
  + introduces sparse linear layer
  + 4-bit Sbox candidate S4 is essentially the PICCOLO Sbox with the last NOT gate at the end being removed
  + one can view the cipher as a primitive in between an SPN and an “ANDrotation-XOR” function like SIMON
  + 8-bit sbox is original and uses 4-input AND-NOR and 4-input OR-NAND gates
  + In order to differentiate the rounds, we simply need a counter, and since the number of rounds of all SKINNY versions is smaller than 64, the most hardware friendly solution is to use a very cheap 6-bit affine LFSR (like in LED)
  + uses 4x4 matrix state as in AES or PRESENT,
  + same shift row of AES, but simpler mixcolumn
- MANTIS:
  + tweakable variant of SKINNY for low-latency implementations. MANTIS uses components from PRINCE and MIDORI
  + replace the PRINCE round function by the MIDORI-round function while keeping the entire design symmetric around the middle to keep the α-reflection property.
     It is actually exactly this PRINCE-like MIDORI that we use as a starting point for designing the low-latency block cipher MANTIS.
  + The cells of the internal state are permuted according to the MIDORI permutation
  + The round constants are generated in a similar way as for PRINCE, that is we used the first digits of π to generate those constants
  + we adopted the linear layer of MIDORI
  + we used the same Sbox as in MIDORI
- GASTON permutation is derived from ASCON permutation by improving its linear layer (more diffusion)
- PRINCE round function basically follows the AES structure, with the exception of using a MixColumns-like mapping with branch number 4 instead of 5.
- MIDORI is an AES-like design

- add GASCON and relation with ASCON
- XXTEA related to XTEA
- RAIDEN related to TEA
- add SM4 relations:
  + similar to AES, Substitution-box (S-box) based on the multiplicative inverse over the same finite mathematical field (GF(2⁸))
  + unbalanced Feistel structure, from whom?
- IDEA from DES??

- add Subterranean
  url can be found here: https://www.researchgate.net/profile/Luc-Claesen/publication/3601817_Subterranean_A_600_Mbitsec_cryptographic_VLSI_chip/links/00b495350dba879660000000/Subterranean-A-600-Mbit-sec-cryptographic-VLSI-chip.pdf
  I have added the pdf: [1993][subterranean,subterranean_hash,subterranean_stream][][].pdf
  Notice that the 2019 Subterranean v2 paper intro gives many relations to be included:
  Subterranean is a cryptographic primitive to be used both for hashing and as a stream
cipher and dates back to 1992 [17,18]. With some imagination its mode can be seen as a
precursor to the sponge [7] with an absorbing phase followed by a squeezing phase.
The round function of Subterranean has features that were adopted in several designs
over the last three decades, including Keccak-p [12] and Xoodoo [21]. Namely, all
its steps, except the addition of a constant, are bit-level shift-invariant operations, its
non-linear step is χ, the mixing step is a lightweight bit-oriented mapping with a heavy
inverse and it has a bit transposition step.
But the Subterranean round function also differs from Keccak-p and Xoodoo in
important ways. Namely, its state is essentially one-dimensional rather than 3-dimensional,
due to the particular transposition and the 257-bit state, it is not software-friendly, and it
has a buffer, similar to the belt in belt-and-mill designs such as RadioGatún [6].
Despite the differences, it only takes some minor refurbishing to turn Subterranean
into a lightweight symmetric cipher suite that can compete with new designs, at least
when implemented in dedicated hardware. Refurbishing we did, and we call the result
Subterranean 2.0. In short, it is Subterranean with the buffer removed and the hashing
and stream encryption modes replaced by a duplex-based construction with modes on
top, inspired by Xoodyak [19].
NOTE: [17] is the 1993 paper, [7] is the 2007 "Sponge functions" Ecrypt Hash Workshop presentation.
