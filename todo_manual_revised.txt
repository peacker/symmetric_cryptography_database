Simplified Manual Todo (2026-08-05)
===================================

Purpose
-------
This file rewrites manual follow-ups into short, clear tasks with simple language.
Use these tasks to finish remaining audit and modeling work.

Task Format
-----------
- Task Name: Short action label
- Description: What to do in plain language
- Notes: Additional notes occasionally added to clarify the task

Open Tasks
----------

1. Task Name: Plan SHA-3 Stub Cleanup
   Description: Create a separate scoped plan for the ~55 SHA-3 round-1-only stub families and prioritize candidates before editing.
   Notes: Deliberately not started this session either -- explicitly flagged as its own project-sized
   piece of work (fetching ~55 individual, often obscure, several publicly-broken competition
   submissions one at a time), not something to fold into a general cleanup pass.

2. Task Name: Confirm GenLowMC Source
   Description: Find a citable source for GenLowMC (author, venue, talk, or draft); if none exists, mark as unpublished and do not model.
   Notes: Third confirmed miss across three separate sessions (2026-07-31, and twice more since).
   Zero results from IACR eprint's own search engine and multiple targeted web searches each time.
   Treat as unpublished/informally named unless you have a specific author/venue/talk lead to search on.

3. Task Name: Archive Zcash Spec PDF
   Description: Optionally save a local archival copy of the exact Zcash protocol version used in notes.
   Notes: Still optional/low-priority; skipped again this session. The reference entry
   (zcash_protocol_specification_2026) already pins the exact spec version read in its venue field,
   so this is purely a "nice to have" archival snapshot, not a correctness gap.

4. Task Name: Verify Unconfirmed Facts
   Description: Re-check secondhand claims (for example AIM/Rain details and some AO round parameters) directly from primary papers before treating them as final.
   Notes: Not revisited this session (2026-08-05) -- still open from the 2026-07-31 AO/PQC-signature
   session. Specifically flagged there as unverified/secondhand: Rain's round function (field
   inversion + affine layer), AIM's S-box (Mersenne-exponent power map), Ciminion/Hydra's exact
   per-round component breakdown, and most of poseidon2/poseidon2b/neptune/grendel/griffin/anemoi/
   reinforced_concrete/polocolo's exact S-box exponents, state-width instance lists, and round counts
   (their primary papers were eprint-blocked with no working mirror found in that session -- worth
   retrying the same non-eprint-mirror strategies that worked for Orthros/Turtle/Mersenne
   Twister/Starkad this session: tosc.iacr.org/tches.iacr.org direct download links, HAL, author
   institutional mirrors, and the Wayback Machine's cached copy of the eprint PDF).

Resolved Since the Last Revision (2026-08-05 session) -- kept here only as a pointer, not a task
--------------------------------------------------------------------------------------------------
Everything else from the previous revision of this file is done; see todo_manual.txt's own dated
entry for 2026-08-05 for the full report (what was added/fixed/decided, files touched, what's still
flagged as unconfirmed within an otherwise-resolved item). Short list for reference: Orthros family +
uLBC edge; Turtle family + Tortuga edge; scrypt edges from POMELO and Tortuga; Mersenne Twister family
+ CryptMT3 edge; LEA construction retag (generalized_feistel -> type3_generalized_feistel); NewDES's
own dedicated round definition (replacing the incorrect des_round reuse); the LOKI/LOKI97
reference_pdfs.py fuzzy-match bug (fixed) and LOKI's own primary paper (fetched); the Crypto Dark
Matter PKC 2021 attacks-and-fixes paper (added as a reference); Starkad (added) and Agrasta (confirmed
already correctly modeled, no change needed); LegendrePRF (added, primitive-tier, type
update_function -- no mode-tier taxonomy extension needed after all) and Pasta (added, mode-tier,
reusing the existing ctr_mode leaf -- no new taxonomy needed there either); ARMADILLO2 cryptanalysis
notes (added, citing both the 2011 MITM and 2012 practical-collision papers); Armadillo's construction
taxonomy (decided -- new data_dependent_permutation_network root, grounded in the real
"data-dependent permutations"/CP-box literature); Skyscraper-v2 (confirmed already resolved in an
earlier session, not touched again); signature schemes out of scope (confirmed, no action needed);
Griffin's GRIFFIN-Sponge (new mode-tier family) and GRIFFIN-Compression (new instance under the
existing primitive-tier griffin family) split; and the "search before adding a construction id"
practice is now also machine-enforced (scripts/validate.py fails the build on any duplicate id across
every *_constructions.yaml/components.yaml/rounds.yaml/etc. catalogue, not just at the SQLite
build-db stage).

Completion Checklist
--------------------
- Run make validate after each grouped change.
- Run make test before closing a task batch.
- Run make build-db (and make build-site when relevant).
- Update scripts/test_reference_pdf_coverage.py KNOWN_MISSING when source coverage changes.
- Commit in small logical units (one concern per commit).
